import { createCanvas, registerFont, loadImage, Canvas } from "canvas";
import { calcShapePoints, roundRect } from "./";
import { GameState, Player, Card, CardSuit, CardValue } from "../models/poker";

// export default function (gameState): Buffer {
//   return Canvas.toBuffer();
// }
